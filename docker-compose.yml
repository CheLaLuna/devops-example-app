version: "3"

services:
  app:
    build: .
    image: chelaluna/devops-example-app

  caddy:
    build:
      context: .
      dockerfile: services/caddy/Dockerfile
    image: chelaluna/devops-example-caddy
